{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "serve": "php artisan serve",
        "brodcast": "php artisan websockets:serve",
        "job-queue": "php artisan queue:work",
        "start": "concurrently \"npm run serve\" \"npm run dev\" \"npm run brodcast\" \"npm run job-queue\""
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "concurrently": "^9.2.0",
        "laravel-echo": "^2.1.7",
        "laravel-vite-plugin": "^1.0.0",
        "pusher-js": "^8.4.0",
        "vite": "^5.0.0"
    }
}
